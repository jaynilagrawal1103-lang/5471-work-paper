node_modules/
dist/bundle.js
.DS_Store
*.log
.env
.env.local
.vercel
.netlify
dist-server/
dist-bundle/

# Client reconciliation outputs — real client financial data, kept out of the repo
5471_Reconciliation_*.xlsx

# OCR service (Python)
ocr-service/.venv/
__pycache__/
.pytest_cache/
.cache/
dist/index.offline.html

# Session scratch harnesses — they hardcode an uploads path for one client run
# and are rebuilt whenever a live document set is being traced.
/.de.ts
/tests/.*.cjs
