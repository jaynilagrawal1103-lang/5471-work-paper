# 5471 Work Paper OCR service — Python 3.10+
# Primary engine (PaddleOCR 3.x; PP-OCRv6/PP-OCRv5 + PP-StructureV3)
paddlepaddle>=3.3,<4
paddleocr>=3.7,<4
# Offline PP-OCRv5 weights through ONNX Runtime (no model download needed)
onnxocr>=3.1
onnxruntime>=1.20
# Secondary engine (optional; large — pulls PyTorch)
# surya-ocr>=0.22
# Final fallback (needs the tesseract binary: apt install tesseract-ocr)
pytesseract>=0.3.13
# Rendering, preprocessing, service
pymupdf>=1.26
opencv-python-headless>=4.10
numpy>=1.26
fastapi>=0.115
uvicorn>=0.30
python-multipart>=0.0.9
